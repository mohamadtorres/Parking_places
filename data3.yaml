# Paths to the train and validation datasets
train: /content/dataset/train/images  # Update with the correct path to your training images
val: /content/dataset/valid/images    # Update with the correct path to your validation images

# Number of classes in your dataset
nc: 2  # Number of classes (adjust based on your dataset)

# Class names (update the class names as per your dataset)
names: ['space-empty', 'space-occupied']
